#include <string>
#include <algorithm>
#include <functional>
#include <iostream>
using namespace std;

int main() {
    string s;
    cin >> s;

    sort(s.begin(), s.end());

    cout << s;
    // 여기에 코드를 작성해주세요.
    return 0;
}